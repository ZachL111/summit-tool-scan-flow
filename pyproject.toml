[project]
name = "summit-tool-scan-flow"
version = "0.1.0"
description = "Package a Python local lab for scan analysis with negative fixtures, human-readable error snapshots, and documented operating limits."
requires-python = ">=3.11"
